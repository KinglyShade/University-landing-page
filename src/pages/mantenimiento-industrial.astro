---
import MainLayout from "../layouts/MainLayout.astro";
import Footer from "../components/vue/footer.vue";
import CareerHero from "../components/vue/CareerHero.vue";
import { careers } from "../DB/db.js";
import "../styles/global.css";

const career = careers.find((c) => c.id === "mantenimiento-industrial");
if (!career) {
  throw new Error("Career 'mantenimiento-industrial' not found in db.js");
}
---

<MainLayout title={`${career.name} - Universidad Tecnológica de Nayarit`}>
  <section id="hero-mantenimiento-industrial" class="relative h-[80vh] md:h-[90vh]">
    <div class="absolute inset-0 bg-black/50 z-10"></div>
    <CareerHero
      client:load
      eyebrow="Oferta educativa"
      title={career.name}
      subtitle={career.description}
      gradientClass="bg-gradient-to-br from-slate-950 via-emerald-900 to-slate-900"
      
    />
  </section>

  <section class="py-16 px-4 sm:px-6 lg:px-8 bg-white text-gray-900">
    <div class="max-w-5xl mx-auto space-y-6 text-sm md:text-base text-gray-700">
      <h2 class="text-2xl md:text-3xl font-bold mb-2">Perfil de la carrera</h2>
      <p>
        Duración: <span class="font-semibold">{career.duration}</span> · Modalidad: <span class="font-semibold">{career.modality}</span>
      </p>
      <p>
        Campo laboral: {career.field}
      </p>
      <p>
        Aquí puedes agregar información de plantas industriales, gestión de mantenimiento, seguridad industrial y proyectos destacados.
      </p>
    </div>
  </section>

  <Footer client:only="vue" />
</MainLayout>